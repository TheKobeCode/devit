import styled from 'styled-components'
import { theme } from '../../styles/theme'

export const Logo = styled.h1`
  display: inline-block;
  font-size: 16px;
  color: ${theme.colors.principalColor};
  background-color: ${theme.colors.superLightColor};
  padding: 2px 10px;
  border-radius: 4px;
`
